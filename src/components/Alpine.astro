---

import Urun from "./Urun4.astro";
---


        <div x-data="{ items: 100 }">
            <div class="flex-container flex-wrap">
                <template x-for="(item, index) in items" :key="index">
                        <Urun/>
                </template>
            </div>
        </div>
        


<style scoped>
.flex-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    align-content: center;
    margin: 0 auto;
    width: 100%;
    max-width: 1200px;
    padding: 0 20px;
}

</style>